name: AmneziaClient
options:
  bundleIdPrefix: org.amnezia
  deploymentTarget:
    iOS: "16.0"
    macOS: "14.0"
  xcodeVersion: "15.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    DEVELOPMENT_TEAM: 3SN795A9D6
    CODE_SIGN_STYLE: Automatic

targets:
  AmneziaCore:
    type: framework
    platform: iOS
    sources:
      - path: AmneziaCore
    info:
      path: AmneziaCore/Info.plist
      properties:
        CFBundlePackageType: FMWK
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.amnezia.core
        DEFINES_MODULE: YES
        SKIP_INSTALL: YES
        GENERATE_INFOPLIST_FILE: NO
        SWIFT_OPTIMIZATION_LEVEL: "-O"
    dependencies:
      - sdk: libz.tbd
      - sdk: Network.framework
      - sdk: NetworkExtension.framework

  AmneziaApp:
    type: application
    platform: iOS
    sources:
      - path: AmneziaApp
    info:
      path: AmneziaApp/Info.plist
      properties:
        CFBundleDisplayName: "Amnezia"
        NSCameraUsageDescription: "Amnezia requires camera access to scan VPN configuration QR codes."
        NSPhotoLibraryUsageDescription: "Amnezia requires photo library access to import QR code images."
        ITSAppUsesNonExemptEncryption: false
        CFBundleURLTypes:
          - CFBundleTypeRole: Viewer
            CFBundleURLName: org.amnezia.client
            CFBundleURLSchemes:
              - vpn
              - amnezia
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
    entitlements:
      path: AmneziaApp/AmneziaApp.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.amnezia.client
        PRODUCT_NAME: "Amnezia"
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: NO
    dependencies:
      - target: AmneziaCore
      - target: AmneziaTunnel
      - sdk: AVFoundation.framework
      - sdk: Vision.framework
      - sdk: PhotosUI.framework

  AmneziaTunnel:
    type: app-extension
    platform: iOS
    sources:
      - path: AmneziaTunnel
    info:
      path: AmneziaTunnel/Info.plist
      properties:
        CFBundleDisplayName: "AmneziaTunnel"
        CFBundlePackageType: "XPC!"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.networkextension.packet-tunnel
          NSExtensionPrincipalClass: $(PRODUCT_MODULE_NAME).PacketTunnelProvider
    entitlements:
      path: AmneziaTunnel/AmneziaTunnel.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.amnezia.client.tunnel
        PRODUCT_NAME: "AmneziaTunnel"
        SKIP_INSTALL: YES
        GENERATE_INFOPLIST_FILE: NO
    dependencies:
      - target: AmneziaCore
      - sdk: NetworkExtension.framework

  AmneziaCoreTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Tests/AmneziaCoreTests
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: AmneziaCore

  AmneziaCoreMac:
    type: framework
    platform: macOS
    sources:
      - path: AmneziaCore
    info:
      path: AmneziaCore/Info.plist
      properties:
        CFBundlePackageType: FMWK
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.amnezia.core.mac
        PRODUCT_NAME: AmneziaCore
        DEFINES_MODULE: YES
        SKIP_INSTALL: YES
        GENERATE_INFOPLIST_FILE: NO
        SWIFT_OPTIMIZATION_LEVEL: "-O"
    dependencies:
      - sdk: libz.tbd
      - sdk: Network.framework
      - sdk: NetworkExtension.framework

  AmneziaMac:
    type: application
    platform: macOS
    sources:
      - path: AmneziaMac
    info:
      path: AmneziaMac/Info.plist
      properties:
        CFBundleDisplayName: "Amnezia"
        CFBundleName: "Amnezia"
        LSApplicationCategoryType: "public.app-category.utilities"
        CFBundleURLTypes:
          - CFBundleTypeRole: Viewer
            CFBundleURLName: org.amnezia.client.mac
            CFBundleURLSchemes:
              - vpn
              - amnezia
    entitlements:
      path: AmneziaMac/AmneziaMac.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.amnezia.client.mac
        PRODUCT_NAME: "Amnezia"
        GENERATE_INFOPLIST_FILE: NO
        COMBINE_HIDPI_IMAGES: YES
        ENABLE_HARDENED_RUNTIME: YES
    dependencies:
      - target: AmneziaCoreMac
      - sdk: Network.framework
      - sdk: NetworkExtension.framework
      - sdk: AppKit.framework
      - sdk: UniformTypeIdentifiers.framework

schemes:
  AmneziaApp:
    build:
      targets:
        AmneziaApp: all
        AmneziaCore: all
        AmneziaTunnel: all
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - AmneziaCoreTests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release

  AmneziaMac:
    build:
      targets:
        AmneziaMac: all
        AmneziaCoreMac: all
    run:
      config: Debug
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
